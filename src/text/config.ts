import {
    truncateText,
    TruncateMode,
    capitalizeFirst,
    toTitleCase,
    removeExtraSpaces,
    reverseString,
    countWords,
    countLetters,
} from './text';

const text = {
    truncateText,
    TruncateMode,
    capitalizeFirst,
    toTitleCase,
    removeExtraSpaces,
    reverseString,
    countWords,
    countLetters,
};

export { text };
