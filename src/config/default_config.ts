export const default_status_config = {
    status_code: true,
    status_message: true,
    status_boolean: true,
};  

export const default_api_config = {
    base_url:'',
};

export const default_s3_config = {
    bucket_name: '',
    access_key_id: '',
    secret_access_key: '',
    region: '',
    project_name: '',
};